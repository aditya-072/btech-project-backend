# btech-project-backend

```sh
pip install "fastapi[all]"
uvicorn main:app --reload
```

```sh
pip install numpy
pip install scikit-learn
```

```sh
pip freeze > requirements.txt
pip install -r requirements.txt
```